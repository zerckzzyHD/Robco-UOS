name: Deploy to GitHub Pages

on:
  # Production is RELEASE-GATED (Protocol 43): this workflow is invoked ONLY by
  # release.yml, and only when a version release (APP_VERSION bump) is detected.
  # It is NOT triggered by ordinary pushes to main, so unreleased work on main
  # (or dev) can never reach production automatically — only a deliberate version
  # bump publishes.
  workflow_call:

  # Manual override for emergency redeploy / rollback from the Actions tab.
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Only allow one deployment at a time
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    # Defense in depth (composes with release.yml's release-only gate + branch
    # protection): when invoked by release.yml the inherited CI run must have
    # succeeded on main; a manual dispatch is always allowed.
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event.workflow_run.conclusion == 'success' &&
       github.event.workflow_run.head_branch == 'main')
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # Deploy the exact commit CI tested; fall back to HEAD for manual dispatch.
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - name: Stage app files
        # Copy only the files that belong on the public site.
        # Private files (CLAUDE.md, RULES.md, ARCHITECTURE.md, tests/, scripts/,
        # .github/, node_modules, package files) are intentionally excluded.
        # assets/ covers icon.png + all shortcut icons (comm-link, inventory, stats, new-campaign).
        run: |
          mkdir -p _site
          cp index.html sw.js manifest.json _site/
          cp -r assets css js _site/
          cp CHANGELOG.md _site/

      - name: Strip dev-only Diagnostic Shell from the production artifact
        # The Diagnostic Shell (js/dev/test-console.js, ~204 KB) is dev/staging-only
        # tooling that production visitors can never open (the unlock minigame ships
        # later). Remove it — and its <script> tag + sw.js precache entry — from the
        # _site/ artifact ONLY (never the repo, never Cloudflare staging, which keeps
        # the shell via cf-staging-build.mjs). The script exits non-zero if any
        # reference survives OR any remaining precache entry dangles, so a half-strip
        # can never publish (all-or-nothing SW precache → black screen). Health-U7.
        run: node scripts/prod-strip-devshell.mjs _site

      - name: Setup Pages
        uses: actions/configure-pages@v6

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
