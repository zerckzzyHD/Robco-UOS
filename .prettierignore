node_modules/
dist/
package-lock.json
*.min.js
*.min.css
scripts/rollback.sh
scripts/pre-commit

# Single-source Node version pin (read by local dev + every CI OS via
# node-version-file). A bare version string — Prettier has no parser for it.
.nvmrc

# Vendored third-party dependency (Visual Upload OCR, Unit 1) — pinned, minified/generated
# files committed byte-for-byte as downloaded. Never reformatted (Prettier reflowing the
# emscripten-generated wasm glue bloats it and risks corrupting the pinned build).
js/vendor/

# Private AI-agent guides — tracked (the test gate reads them) but hand-authored,
# not Prettier-managed. They were formerly skipped via .gitignore; keep them skipped
# here now that they are un-ignored so Prettier does not churn their tables.
CLAUDE.md

# Claude-facing local reference library (gitignored; belt-and-suspenders so Prettier
# never churns the hand-authored brain dump even if the .gitignore default changes).
library/
